{
  "name": "demo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "serve . --listen 3000",
    "build": "echo 'No build step needed'",
    "clean": "rm -f echo-box.js*"
  },
  "dependencies": {
    "echo-box": "workspace:*"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
